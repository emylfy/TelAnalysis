# Python dependencies for TelAnalysis (React SPA + FastAPI backend).
# The frontend is built separately with npm (see run.sh / README).

# Web backend
fastapi>=0.136
uvicorn[standard]>=0.47
python-multipart>=0.0.20   # required by FastAPI's UploadFile / File()

# Data processing + analytics
networkx==3.6.1            # reply-graph community detection (Louvain)
nltk==3.9.4
jmespath==1.1.0

# Wordcloud rendering
matplotlib==3.10.9
wordcloud==1.9.6

# Content extraction
emoji==2.15.0
phonenumbers==9.0.30
validate_email==1.3

# Optional: install separately for Russian sentiment analysis (~1GB)
# pip install -r requirements-sentiment.txt
