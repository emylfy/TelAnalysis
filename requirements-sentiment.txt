# Optional dependencies for Russian sentiment analysis.
# Adds ~1GB to install (torch + transformers).
# Model 'seara/rubert-tiny2-russian-sentiment' is ~50MB, downloaded on first
# use to the HuggingFace cache.
#
# Install with:
#   pip install -r requirements-sentiment.txt
#
# After install, restart the app. The sentiment scores and chat-wide averages
# will populate automatically.

transformers==5.8.0
torch==2.11.0
